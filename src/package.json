{
  "name": "hello_world",
  "version": "1.0.0",
  "description": "hello world sample for NodeJS",
  "main": "src/index.js",
  "repository": "https://github.com/awslabs/aws-sam-cli/tree/develop/samcli/local/init/templates/cookiecutter-aws-sam-hello-nodejs",
  "author": "SAM CLI",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0"
  },
  "scripts": {
    "start": "cd ..; sam local start-api",
    "build": "cd ..; sam package --output-template-file packaged.yaml --s3-bucket govtalk",
    "deploy": "cd ..; sam deploy --template-file packaged.yaml --stack-name GovTalk --capabilities CAPABILITY_IAM --region ap-southeast-1",
    "test": "mocha tests/unit/"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.1.1"
  }
}
